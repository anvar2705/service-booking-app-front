import { SxStyles } from "@shared/utils";

export default {
    container: {
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flex: 1,
    },
} satisfies SxStyles;
